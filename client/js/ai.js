// Placeholder — implemented in S2.1
class AIPlayer {
  constructor(logic) { this.logic = logic; }
  findMove(allPlayerMoves, mustJump) { return null; }
}

if (typeof module !== 'undefined') module.exports = { AIPlayer };
